import { Stack, styled } from "@mui/material";

export const InfoCard = styled(Stack)(() => ({
    backgroundColor: "rgba(66, 38, 128, 0.05)",
    padding: "16px",
    gap: 5,
    borderRadius: 5,
    boxShadow: "0 1px 2px rgba(0, 0, 0, 0.38);"
}))